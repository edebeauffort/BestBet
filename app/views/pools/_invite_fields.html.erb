<div class="nested-fields">
  <%= f.association :user, prompt: "invite a friend", label: false, collection: (User.all.where.not(id: current_user.id)), label_method: ->(obj){ obj.email } %>
  <%= link_to_remove_association f do %>
    <i class='fa fa-times' id='remove-invite'></i>
  <% end %>
</div>
